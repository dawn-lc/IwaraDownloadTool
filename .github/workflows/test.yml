name: TEST
on:
  workflow_dispatch:
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print the input tag to STDOUT
        run: echo  The tags are ${{ github.actor }} 
      - name: Print the input tag to STDOUT
        run: echo  The tags are ${{ github.actor_id }} 
